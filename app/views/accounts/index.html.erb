<h1>Listing accounts</h1>

<table>
  <tr>
    <th><%=t :username %></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @accounts.each do |account| %>
  <tr>
    <td><%= account.username %></td>
    <td><%= link_to 'Show', account %></td>
    <td><%= link_to 'Edit', edit_account_path(account) %></td>
    <td><%= link_to 'Destroy', account, confirm: 'Are you sure?', method: :delete %></td>
    <td><%= link_to 'Profile', profiles_path+'/'+account.account_id %></td>
    <td><%= link_to 'Spots Owner', spots_path+'/owner'+'/'+account.account_id %></td>
    <td><%= link_to 'Spots Joined', spots_path+'/joined'+'/'+account.account_id %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Account', new_account_path %><br />
<%= link_to 'New Spot', new_spot_path %>
