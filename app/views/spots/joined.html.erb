<h1>Joined spots</h1>

<table>
  <tr>
    <th>Spots</th>
    <th></th>
  </tr>

<% @spots.each do |spot| %>
  <tr>
    <td><%= (Spot.find_by_spot_id (spot.spot_id)).local %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Spot', new_spot_path %>
